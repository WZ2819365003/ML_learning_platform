# 时序模型接入 V3 平台 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把时序预测作为第三个一等 family `ts` 接入 V3 ModelingTask → Experiment → Run 主链路，落地 5 个模型（ARIMA / ETS / LSTM-forecaster / TCN-forecaster / TimesFM 1.0）。

**Architecture:** `ts_registry` + `BaseTSTrainer` 抽象与 ml/dl 同构；新 `task_type=forecasting`；不新增数据库表（time_series 元数据走 TrainingPlan.payload）；执行链复用 `register_executor("ts_train", ...)` + `tuning_service` 加 ts family 分支。

**Tech Stack:** Python 3.13 (FastAPI 主进程) + Python 3.10 venv (TimesFM 子进程) + statsmodels（新增）+ torch 2.6 + React 18 + ECharts。

**关联设计文档:** `doc/specs/2026-04-29-ts-into-v3-design.md`

**总工作量估算:** ~8 dev-days，33 个任务，分 8 个里程碑。

---

## 📋 文件清单

### 新增

```
ml_platform/app/core/ts_registry.py
ml_platform/app/core/ts_trainer.py
ml_platform/app/core/dl_models/lstm_forecaster.py
ml_platform/app/core/dl_models/tcn_forecaster.py
ml_platform/app/core/ts_metrics.py
ml_platform/app/services/ts_service.py
ml_platform/app/api/routes/ts_v3.py
ml_platform/tests/test_ts_registry.py
ml_platform/tests/test_ts_metrics.py
ml_platform/tests/test_ts_trainer_arima.py
ml_platform/tests/test_ts_trainer_ets.py
ml_platform/tests/test_ts_trainer_lstm_forecaster.py
ml_platform/tests/test_ts_trainer_tcn_forecaster.py
ml_platform/tests/test_ts_trainer_timesfm.py
ml_platform/tests/test_ts_service_executor.py
ml_platform_web/src/components/forecasting/TimeSeriesPlanForm.jsx
ml_platform_web/src/components/forecasting/ForecastChart.jsx
ml_platform_web/src/components/forecasting/ForecastMetrics.jsx
ml_platform_web/src/components/forecasting/ColumnSelector.jsx
ml_platform_web/src/components/forecasting/ValidationConfig.jsx
playwright_test/test/13-v3-forecasting.spec.js
examples/data/sin_wave_daily.csv
```

### 修改

```
ml_platform/requirements.txt             — 加 statsmodels>=0.14
ml_platform/app/core/model_registry.py   — resolve_model_family 加 ts 分支
ml_platform/app/models/schemas.py        — TaskType 加 forecasting + TimeSeriesPlanConfig
ml_platform/app/services/resolver.py     — 多扩展名兼容 + is_forecaster
ml_platform/app/services/tuning_service.py — trial 派发加 family=='ts' 分支
ml_platform/app/services/platform_task_detail_service.py — forecasting metrics/charts 分支
ml_platform/app/api/routes/visualization.py — forecasting 图表端点
ml_platform/app/main.py                  — 注册 ts_v3_router
ml_platform_web/src/pages/ModelingTaskDetail.jsx — 任务类型加 forecasting
ml_platform_web/src/pages/V3Runs.jsx     — family=ts 过滤
ml_platform_web/src/pages/TSConfig.jsx   — 加 deprecated banner
ml_platform_web/src/pages/TSMonitor.jsx  — 加 deprecated banner
ml_platform_web/src/pages/TSResults.jsx  — 加 deprecated banner
ml_platform_web/src/services/api.js      — 加 ts_v3 endpoints
doc/功能说明.md                           — 加 forecasting 章节
doc/系统架构.md                           — 更新 family 列表为 ml/dl/ts
ml_platform_web/package.json             — 版本 → 3.4.0
```

---

# M1 — 镜像 rebuild for statsmodels

## Task 1: 添加 statsmodels 到 requirements.txt

**Files:**
- Modify: `ml_platform/requirements.txt`

- [ ] **Step 1: 在 requirements.txt 末尾追加 statsmodels**

```bash
echo "statsmodels==0.14.4" >> ml_platform/requirements.txt
```

- [ ] **Step 2: 校验位置**

```bash
tail -3 ml_platform/requirements.txt
```

期望末三行包含 `statsmodels==0.14.4`。

- [ ] **Step 3: Commit**

```bash
git add ml_platform/requirements.txt
git commit -m "build(v3.4): add statsmodels==0.14.4 for ARIMA/ETS"
```

## Task 2: 重建 backend 镜像并验证

**Files:** 无代码改动；仅基础设施。

- [ ] **Step 1: rebuild backend 镜像**

```bash
docker compose -f docker/docker-compose.yml build backend
```

期望最后一行为 `Successfully tagged docker-backend:latest` 或 build 完成无 error。

- [ ] **Step 2: restart backend**

```bash
docker compose -f docker/docker-compose.yml up -d backend
```

- [ ] **Step 3: 等 healthy**

```bash
sleep 15 && docker compose -f docker/docker-compose.yml ps backend
```

期望状态包含 `(healthy)`。

- [ ] **Step 4: 验证 statsmodels 可导入**

```bash
docker exec ml_platform_backend python -c "import statsmodels; print('OK', statsmodels.__version__)"
```

期望输出：`OK 0.14.x`。

- [ ] **Step 5: 跑现有 67 spec 回归确认没踩**

```bash
cd playwright_test && npx playwright test --project=chromium 2>&1 | tail -20
```

期望 67/67 通过。

- [ ] **Step 6: 无新文件，跳过 commit**

---

# M2 — 后端骨架

## Task 3: 新增 ts_registry.py（5 个模型 stub）

**Files:**
- Create: `ml_platform/app/core/ts_registry.py`

- [ ] **Step 1: 创建文件，写入完整内容**

```python
"""TS Model Registry — single source of truth for time-series model metadata.

Mirrors dl_registry.py / model_registry.py shape. 5 models in scope:
  arima              — statsmodels SARIMAX
  ets                — statsmodels Holt-Winters / ETS
  lstm_forecaster    — pytorch direct multi-step
  tcn_forecaster     — pytorch causal conv stack
  timesfm_1          — google foundation model via 3.10 venv subprocess
"""
from __future__ import annotations
from pathlib import Path

TS_CATEGORY_REGISTRY: list[dict] = [
    {"id": "statistical", "display_name": "统计模型", "icon": "line-chart"},
    {"id": "neural",      "display_name": "神经网络", "icon": "deployment-unit"},
    {"id": "foundation",  "display_name": "基础模型", "icon": "cloud"},
]

TS_MODEL_REGISTRY: list[dict] = [
    {
        "id": "arima", "display_name": "ARIMA",
        "category": "statistical", "task_types": ["forecasting"],
        "supports_intervals": True, "supports_exogenous": True,
        "params": [
            {"name": "p", "display_name": "AR 阶数 p", "type": "int",
             "default": 1, "min": 0, "max": 10, "required": True, "advanced": False,
             "description": "自回归阶数"},
            {"name": "d", "display_name": "差分阶数 d", "type": "int",
             "default": 1, "min": 0, "max": 2, "required": True, "advanced": False,
             "description": "差分次数（去趋势）"},
            {"name": "q", "display_name": "MA 阶数 q", "type": "int",
             "default": 1, "min": 0, "max": 10, "required": True, "advanced": False,
             "description": "移动平均阶数"},
            {"name": "seasonal_periods", "display_name": "季节周期", "type": "int",
             "default": 0, "min": 0, "max": 365, "required": False, "advanced": True,
             "description": "0 关闭季节性；7=周；12=月；52=年"},
        ],
    },
    {
        "id": "ets", "display_name": "ETS / Holt-Winters",
        "category": "statistical", "task_types": ["forecasting"],
        "supports_intervals": True, "supports_exogenous": False,
        "params": [
            {"name": "trend", "display_name": "趋势", "type": "str",
             "default": "add", "options": ["add", "mul", "none"],
             "required": True, "advanced": False,
             "description": "additive/multiplicative/none"},
            {"name": "seasonal", "display_name": "季节性", "type": "str",
             "default": "none", "options": ["add", "mul", "none"],
             "required": True, "advanced": False, "description": ""},
            {"name": "seasonal_periods", "display_name": "季节周期", "type": "int",
             "default": 0, "min": 0, "max": 365, "required": False, "advanced": False,
             "description": "0 关闭"},
        ],
    },
    {
        "id": "lstm_forecaster", "display_name": "LSTM Forecaster",
        "category": "neural", "task_types": ["forecasting"],
        "supports_intervals": False, "supports_exogenous": True,
        "params": [
            {"name": "hidden_size", "display_name": "隐藏维度", "type": "int",
             "default": 64, "min": 8, "max": 512, "required": True, "advanced": False,
             "description": ""},
            {"name": "num_layers", "display_name": "层数", "type": "int",
             "default": 2, "min": 1, "max": 6, "required": True, "advanced": False,
             "description": ""},
            {"name": "dropout", "display_name": "Dropout", "type": "float",
             "default": 0.1, "min": 0.0, "max": 0.5, "step": 0.05,
             "required": False, "advanced": False, "description": ""},
            {"name": "epochs", "display_name": "训练轮数", "type": "int",
             "default": 50, "min": 1, "max": 500, "required": True, "advanced": False,
             "description": ""},
            {"name": "batch_size", "display_name": "批大小", "type": "int",
             "default": 32, "min": 8, "max": 512, "required": True, "advanced": False,
             "description": ""},
            {"name": "learning_rate", "display_name": "学习率", "type": "float",
             "default": 0.001, "min": 1e-5, "max": 0.1, "step": 0.0001,
             "required": True, "advanced": False, "description": ""},
        ],
    },
    {
        "id": "tcn_forecaster", "display_name": "TCN Forecaster",
        "category": "neural", "task_types": ["forecasting"],
        "supports_intervals": False, "supports_exogenous": True,
        "params": [
            {"name": "channels", "display_name": "通道数", "type": "int",
             "default": 32, "min": 8, "max": 256, "required": True, "advanced": False,
             "description": ""},
            {"name": "kernel_size", "display_name": "卷积核大小", "type": "int",
             "default": 3, "min": 2, "max": 7, "required": True, "advanced": False,
             "description": ""},
            {"name": "num_layers", "display_name": "层数", "type": "int",
             "default": 4, "min": 2, "max": 10, "required": True, "advanced": False,
             "description": ""},
            {"name": "dropout", "display_name": "Dropout", "type": "float",
             "default": 0.1, "min": 0.0, "max": 0.5, "step": 0.05,
             "required": False, "advanced": False, "description": ""},
            {"name": "epochs", "display_name": "训练轮数", "type": "int",
             "default": 50, "min": 1, "max": 500, "required": True, "advanced": False,
             "description": ""},
            {"name": "batch_size", "display_name": "批大小", "type": "int",
             "default": 32, "min": 8, "max": 512, "required": True, "advanced": False,
             "description": ""},
            {"name": "learning_rate", "display_name": "学习率", "type": "float",
             "default": 0.001, "min": 1e-5, "max": 0.1, "step": 0.0001,
             "required": True, "advanced": False, "description": ""},
        ],
    },
    {
        "id": "timesfm_1", "display_name": "TimesFM 1.0 (Google)",
        "category": "foundation", "task_types": ["forecasting"],
        "supports_intervals": False, "supports_exogenous": False,
        "params": [
            {"name": "context_len", "display_name": "上下文长度", "type": "int",
             "default": 512, "min": 32, "max": 2048, "required": True, "advanced": False,
             "description": "TimesFM 输入序列长度"},
        ],
        "availability_check": "timesfm_env",
    },
]


def get_ts_model_spec(model_id: str) -> dict | None:
    return next((m for m in TS_MODEL_REGISTRY if m["id"] == model_id), None)


def get_ts_model_ids() -> list[str]:
    return [m["id"] for m in TS_MODEL_REGISTRY]


def list_available_models() -> list[dict]:
    """Return registry entries with `available: bool` flag.

    For models requiring external resources (timesfm_env), checks at call time.
    """
    out = []
    for m in TS_MODEL_REGISTRY:
        avail = True
        check = m.get("availability_check")
        if check == "timesfm_env":
            venv_python = (
                Path(__file__).parent.parent.parent / "timesfm_env" / "Scripts" / "python.exe"
            )
            avail = venv_python.exists()
        out.append({**m, "available": avail})
    return out
```

- [ ] **Step 2: 校验文件被正确写入**

```bash
docker exec ml_platform_backend python -c "from app.core.ts_registry import TS_MODEL_REGISTRY, get_ts_model_spec; print(len(TS_MODEL_REGISTRY), [m['id'] for m in TS_MODEL_REGISTRY])"
```

期望：`5 ['arima', 'ets', 'lstm_forecaster', 'tcn_forecaster', 'timesfm_1']`

注：因 backend 没有 bind mount，必须 `docker compose restart backend` 让新文件可见。

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
```

然后再跑校验。

- [ ] **Step 3: Commit**

```bash
git add ml_platform/app/core/ts_registry.py
git commit -m "feat(v3.4): ts_registry with 5 models (ARIMA/ETS/LSTM-fc/TCN-fc/TimesFM)"
```

## Task 4: 写 ts_registry 单元测试

**Files:**
- Create: `ml_platform/tests/test_ts_registry.py`

- [ ] **Step 1: 写测试**

```python
from app.core.ts_registry import (
    TS_MODEL_REGISTRY,
    get_ts_model_spec,
    get_ts_model_ids,
    list_available_models,
)


def test_5_models_registered():
    ids = get_ts_model_ids()
    assert ids == ["arima", "ets", "lstm_forecaster", "tcn_forecaster", "timesfm_1"]


def test_each_model_has_required_fields():
    required = {"id", "display_name", "category", "task_types", "params",
                "supports_intervals", "supports_exogenous"}
    for m in TS_MODEL_REGISTRY:
        missing = required - m.keys()
        assert not missing, f"{m['id']} missing: {missing}"


def test_arima_supports_intervals_and_exog():
    m = get_ts_model_spec("arima")
    assert m["supports_intervals"] is True
    assert m["supports_exogenous"] is True


def test_lstm_does_not_support_intervals_baseline():
    m = get_ts_model_spec("lstm_forecaster")
    assert m["supports_intervals"] is False


def test_unknown_id_returns_none():
    assert get_ts_model_spec("not_a_model") is None


def test_list_available_marks_timesfm_status():
    avail = list_available_models()
    timesfm = next(m for m in avail if m["id"] == "timesfm_1")
    assert "available" in timesfm
    assert isinstance(timesfm["available"], bool)
```

- [ ] **Step 2: 跑测试，期望全 PASS**

```bash
docker exec ml_platform_backend python -m pytest tests/test_ts_registry.py -v
```

期望：6 passed。

- [ ] **Step 3: Commit**

```bash
git add ml_platform/tests/test_ts_registry.py
git commit -m "test(v3.4): ts_registry unit tests (6 cases)"
```

## Task 5: 新增 BaseTSTrainer 抽象基类 + ForecastResult

**Files:**
- Create: `ml_platform/app/core/ts_trainer.py`

- [ ] **Step 1: 写抽象基类**

```python
"""BaseTSTrainer — abstract contract for all ts family trainers."""
from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from pathlib import Path
from typing import Any

import numpy as np
import pandas as pd


@dataclass
class TSMeta:
    """Static metadata describing the time-series problem.

    Built once by ts_service from TrainingPlan.payload.time_series and
    handed to every trainer's fit() call.
    """
    timestamp_col: str
    target_col: str
    series_id_col: str | None
    exogenous_cols: list[str]
    freq: str
    horizon: int
    lookback: int
    interval_levels: list[int] = field(default_factory=lambda: [80, 95])


@dataclass
class ForecastResult:
    """Trainer.predict() output.

    mean: shape (horizon,) — point forecast
    intervals: dict[level, (low, high)] each shape (horizon,) — None if model has no interval support
    """
    mean: np.ndarray
    intervals: dict[int, tuple[np.ndarray, np.ndarray]] | None = None


class BaseTSTrainer(ABC):
    """Every ts family trainer must satisfy this contract."""

    name: str
    supports_intervals: bool = False
    supports_exogenous: bool = False

    @abstractmethod
    def fit(self, train_df: pd.DataFrame, meta: TSMeta, params: dict[str, Any]) -> None: ...

    @abstractmethod
    def predict(
        self, horizon: int, exog: pd.DataFrame | None = None
    ) -> ForecastResult: ...

    @abstractmethod
    def save(self, path: Path) -> None: ...

    @classmethod
    @abstractmethod
    def load(cls, path: Path) -> "BaseTSTrainer": ...
```

- [ ] **Step 2: 校验导入**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -c "from app.core.ts_trainer import BaseTSTrainer, TSMeta, ForecastResult; print('OK')"
```

期望：`OK`。

- [ ] **Step 3: Commit**

```bash
git add ml_platform/app/core/ts_trainer.py
git commit -m "feat(v3.4): BaseTSTrainer abstract contract + TSMeta/ForecastResult"
```

## Task 6: 加 task_type=forecasting 到 schemas + TimeSeriesPlanConfig

**Files:**
- Modify: `ml_platform/app/models/schemas.py`

- [ ] **Step 1: 找到 TaskType 定义**

```bash
grep -n "TaskType\|task_type" ml_platform/app/models/schemas.py | head
```

记录行号。

- [ ] **Step 2: 在 schemas.py 中扩展 TaskType 并新增 TimeSeriesPlanConfig**

在文件 import 区下方追加（如 TaskType 已存在则修改其值）：

```python
from typing import Literal

TaskType = Literal["classification", "regression", "forecasting"]


class TimeSeriesValidationConfig(BaseModel):
    method: Literal["holdout", "rolling_origin", "expanding_window"] = "holdout"
    test_size: int = Field(gt=0, default=14)
    step: int = Field(gt=0, default=1)


class TimeSeriesPlanConfig(BaseModel):
    timestamp_col: str
    target_col: str
    series_id_col: str | None = None
    exogenous_cols: list[str] = []
    freq: str
    horizon: int = Field(gt=0, le=10000)
    lookback: int = Field(gt=0, default=28)
    validation: TimeSeriesValidationConfig = Field(default_factory=TimeSeriesValidationConfig)
    interval_levels: list[int] = [80, 95]
```

如果 schemas.py 已有 `TaskType`，修改其 Literal 值；否则按上面整段插入到合适位置（建议靠近其他 Literal 类型定义）。

- [ ] **Step 3: 校验导入**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -c "from app.models.schemas import TimeSeriesPlanConfig; m = TimeSeriesPlanConfig(timestamp_col='ds', target_col='y', freq='D', horizon=14); print('OK', m.horizon)"
```

期望：`OK 14`。

- [ ] **Step 4: Commit**

```bash
git add ml_platform/app/models/schemas.py
git commit -m "feat(v3.4): TaskType=forecasting + TimeSeriesPlanConfig schema"
```

## Task 7: 在 model_registry 加 ts family 分支

**Files:**
- Modify: `ml_platform/app/core/model_registry.py`（找到 `def resolve_model_family` 与 `def list_models`）

- [ ] **Step 1: 修改 resolve_model_family**

找到现有：

```python
def resolve_model_family(model_id: str) -> str | None:
    """Return 'ml', 'dl', or None if the model id is not in either registry."""
    from app.core.dl_registry import get_dl_model_spec  # local import
```

替换为：

```python
def resolve_model_family(model_id: str) -> str | None:
    """Return 'ml', 'dl', 'ts', or None if the model id is not in any registry."""
    from app.core.dl_registry import get_dl_model_spec  # local import
    from app.core.ts_registry import get_ts_model_spec  # local import

    if get_ml_model_spec(model_id) is not None:
        return "ml"
    if get_dl_model_spec(model_id) is not None:
        return "dl"
    if get_ts_model_spec(model_id) is not None:
        return "ts"
    return None
```

注意：保留原始函数体的 ml/dl 检查；只在末尾加 ts 一段。如果原代码长得不一样，沿用其分支风格。

- [ ] **Step 2: 修改 list_models 接受 family="ts"**

找到 `def list_models(...)` —— 在末尾 family 分支加：

```python
    if family in (None, "ts"):
        from app.core.ts_registry import TS_MODEL_REGISTRY
        for m in TS_MODEL_REGISTRY:
            out.append({**m, "family": "ts"})
```

- [ ] **Step 3: 校验**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -c "from app.core.model_registry import resolve_model_family; print(resolve_model_family('arima'), resolve_model_family('lstm_forecaster'), resolve_model_family('not_a_model'))"
```

期望：`ts ts None`。

- [ ] **Step 4: Commit**

```bash
git add ml_platform/app/core/model_registry.py
git commit -m "feat(v3.4): model_registry recognises ts family"
```

## Task 8: 让 resolver.py 支持多扩展名 + is_forecaster

**Files:**
- Modify: `ml_platform/app/services/resolver.py`

- [ ] **Step 1: 找到硬编码的 .joblib**

```bash
grep -n '\.joblib' ml_platform/app/services/resolver.py
```

预计三处：line ~350、~411、~428。

- [ ] **Step 2: 加 helper `_resolve_model_file`**

在 `load_model` 函数附近（约 line 125）插入：

```python
_MODEL_EXTENSIONS = (".joblib", ".pt", ".json")


def _find_model_file(models_dir, base_id: str):
    """Search storage/models/ for {base_id}.{joblib,pt,json} — first match wins."""
    for ext in _MODEL_EXTENSIONS:
        candidate = models_dir / f"{base_id}{ext}"
        if candidate.exists():
            return candidate, ext
    return None, None
```

然后在 line ~350 处替换 `candidate = settings.storage_models / f"{cid}.joblib"` 段落为：

```python
        candidate, ext = _find_model_file(settings.storage_models, cid)
        if candidate:
            model_path = f"storage/models/{cid}{ext}"
```

同样替换 line ~411、~428 的 `.joblib` 硬编码处。

- [ ] **Step 3: 加 is_forecaster**

在 `is_regressor` 之后追加：

```python
FORECASTER_TOKENS: set[str] = {
    "arima", "ets", "lstm_forecaster", "tcn_forecaster", "timesfm_1",
}


def is_forecaster(model_type: str | None) -> bool:
    if not model_type:
        return False
    return model_type.lower() in FORECASTER_TOKENS
```

- [ ] **Step 4: 修改 task_kind_for**

```python
def task_kind_for(model_type: str | None) -> str:
    """Return 'classification' | 'regression' | 'forecasting'."""
    if is_forecaster(model_type):
        return "forecasting"
    return "regression" if is_regressor(model_type) else "classification"
```

- [ ] **Step 5: 校验**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -c "from app.services.resolver import is_forecaster, task_kind_for; print(is_forecaster('arima'), task_kind_for('lstm_forecaster'), task_kind_for('xgboost_classifier'))"
```

期望：`True forecasting classification`。

- [ ] **Step 6: 跑现有 viz 单测**

```bash
docker exec ml_platform_backend python -m pytest tests/ -k "viz or resolver" -v 2>&1 | tail -30
```

期望全绿。如有 fail，回看修改的 .joblib 替换处是否破坏了现有路径。

- [ ] **Step 7: Commit**

```bash
git add ml_platform/app/services/resolver.py
git commit -m "feat(v3.4): resolver multi-extension + is_forecaster + forecasting task_kind"
```

## Task 9: 新增 ts_service.py 骨架 + register_executor

**Files:**
- Create: `ml_platform/app/services/ts_service.py`

- [ ] **Step 1: 写骨架文件**

```python
"""TS Service — orchestrates ts family training within V3 platform.

Responsibilities:
  * Load Dataset → DataFrame
  * Validate time_series payload
  * Slice by validation strategy
  * Instantiate trainer (via ts_registry)
  * fit → predict on val → compute FORECAST_EVAL_METRICS
  * Persist model + write ExperimentRunLog + metrics_snapshot
  * Register executor("ts_train", ...) for V3 dispatch chain
"""
from __future__ import annotations

import logging
from pathlib import Path
from typing import Any

import pandas as pd

from app.scheduler.executors import register_executor

logger = logging.getLogger(__name__)


async def run_ts_executor(domain_id: str, platform_task_id: str) -> dict[str, Any]:
    """V3 executor for ts family.

    Contract: domain_id is ExperimentRun.id (matches ml/dl convention).
    """
    # NOTE: full implementation lands in Task 16 (after trainers exist).
    # This stub raises so any premature dispatch is loud.
    raise NotImplementedError(
        "ts_service.run_ts_executor: stub. Real implementation arrives in Task 16."
    )


register_executor("ts_train", run_ts_executor)
```

- [ ] **Step 2: 在 main.py 确保 ts_service 被 import 一次（让 register_executor 生效）**

在 `app/main.py` 顶层（与其他 service import 同区域）追加：

```python
from app.services import ts_service  # noqa: F401  — register_executor side effect
```

- [ ] **Step 3: 校验 register**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -c "from app.scheduler.executors import has_executor, list_registered_kinds; print(has_executor('ts_train'), list_registered_kinds())"
```

期望：`True [..., 'ts_train', ...]`

- [ ] **Step 4: Commit**

```bash
git add ml_platform/app/services/ts_service.py ml_platform/app/main.py
git commit -m "feat(v3.4): ts_service stub registers executor('ts_train')"
```

## Task 10: 在 tuning_service 加 family=='ts' 分支

**Files:**
- Modify: `ml_platform/app/services/tuning_service.py`

- [ ] **Step 1: 找到所有 family 判断处**

```bash
grep -n "family == 'dl'\|family=='dl'" ml_platform/app/services/tuning_service.py
```

预计 4-5 处。

- [ ] **Step 2: 在每处 dl 分支后加平行的 ts 分支**

例：在 `_persist_trials` 中找到：

```python
        if family == "dl":
            # ...dl-specific persist logic
        else:
            # ...ml default
```

改为：

```python
        if family == "dl":
            # ...dl-specific persist logic
        elif family == "ts":
            # ts uses the same trial-row shape as dl (kind='ts_train' on the
            # PlatformTask side). The registry-aware payload is built inside
            # ts_service.run_ts_executor, so here we only mirror the dl branch.
            # ...same as dl branch (replace kind='ts_train')
        else:
            # ...ml default
```

每处都按这个模式：dl 怎么干，ts 就怎么干，**只把 PlatformTask.kind 换成 `'ts_train'`**。

- [ ] **Step 3: 校验语法**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -c "from app.services.tuning_service import _persist_trials; print('OK')"
```

期望：`OK`。

- [ ] **Step 4: Commit**

```bash
git add ml_platform/app/services/tuning_service.py
git commit -m "feat(v3.4): tuning_service dispatches family='ts' to ts_train executor"
```

---

# M3 — ARIMA + ETS

## Task 11: 写 ts_metrics.py（forecasting 专属指标）

**Files:**
- Create: `ml_platform/app/core/ts_metrics.py`
- Create: `ml_platform/tests/test_ts_metrics.py`

- [ ] **Step 1: 写测试先**

```python
# tests/test_ts_metrics.py
import numpy as np
from app.core.ts_metrics import mae, rmse, mape, smape, mase, coverage


def test_mae_basic():
    y = np.array([1.0, 2.0, 3.0])
    p = np.array([1.0, 2.5, 2.5])
    assert abs(mae(y, p) - (0 + 0.5 + 0.5) / 3) < 1e-9


def test_rmse_basic():
    y = np.array([0.0, 0.0])
    p = np.array([3.0, 4.0])
    assert abs(rmse(y, p) - (np.sqrt((9 + 16) / 2))) < 1e-9


def test_mape_handles_zero_truth():
    y = np.array([0.0, 1.0, 2.0])
    p = np.array([0.5, 1.0, 1.0])
    val = mape(y, p)
    assert val >= 0 and not np.isnan(val)


def test_smape_symmetric():
    y = np.array([2.0, 4.0])
    p = np.array([4.0, 2.0])
    a = smape(y, p)
    assert 0 < a <= 200


def test_mase_uses_seasonal_naive():
    # constant series → seasonal naive perfect → MASE undefined; use 1-period
    train = np.array([1, 2, 3, 4, 5, 6])
    y = np.array([7, 8])
    p = np.array([7, 8])
    val = mase(y, p, train, season=1)
    assert abs(val - 0.0) < 1e-9


def test_coverage_full():
    y = np.array([1.0, 2.0, 3.0])
    lo = np.array([0.5, 1.5, 2.5])
    hi = np.array([1.5, 2.5, 3.5])
    assert abs(coverage(y, lo, hi) - 1.0) < 1e-9
```

- [ ] **Step 2: 跑测试，期望 fail**

```bash
docker exec ml_platform_backend python -m pytest tests/test_ts_metrics.py -v
```

期望：所有 case ImportError。

- [ ] **Step 3: 写实现**

```python
# app/core/ts_metrics.py
"""Forecasting-specific metrics — independent of regression metrics module."""
from __future__ import annotations

import numpy as np


def mae(y_true: np.ndarray, y_pred: np.ndarray) -> float:
    return float(np.mean(np.abs(y_true - y_pred)))


def rmse(y_true: np.ndarray, y_pred: np.ndarray) -> float:
    return float(np.sqrt(np.mean((y_true - y_pred) ** 2)))


def mape(y_true: np.ndarray, y_pred: np.ndarray) -> float:
    """% error; epsilon prevents div-by-zero on truth==0 elements."""
    eps = 1e-9
    return float(np.mean(np.abs((y_true - y_pred) / (np.abs(y_true) + eps))) * 100.0)


def smape(y_true: np.ndarray, y_pred: np.ndarray) -> float:
    """Symmetric MAPE in [0, 200]."""
    denom = (np.abs(y_true) + np.abs(y_pred)) / 2.0
    eps = 1e-9
    return float(np.mean(np.abs(y_true - y_pred) / (denom + eps)) * 100.0)


def mase(y_true: np.ndarray, y_pred: np.ndarray,
         train: np.ndarray, season: int = 1) -> float:
    """Mean Absolute Scaled Error — relative to seasonal naive on train."""
    if len(train) <= season:
        return float("nan")
    naive_err = np.mean(np.abs(train[season:] - train[:-season]))
    if naive_err < 1e-12:
        return 0.0 if mae(y_true, y_pred) < 1e-12 else float("inf")
    return float(np.mean(np.abs(y_true - y_pred)) / naive_err)


def coverage(y_true: np.ndarray, low: np.ndarray, high: np.ndarray) -> float:
    """Fraction of true values that fall within [low, high]."""
    return float(np.mean((y_true >= low) & (y_true <= high)))


FORECAST_EVAL_METRICS: list[dict] = [
    {"value": "mae",  "label": "MAE"},
    {"value": "rmse", "label": "RMSE"},
    {"value": "mape", "label": "MAPE (%)"},
    {"value": "smape","label": "sMAPE (%)"},
    {"value": "mase", "label": "MASE"},
    {"value": "coverage_80", "label": "80% 区间覆盖率"},
    {"value": "coverage_95", "label": "95% 区间覆盖率"},
]
```

- [ ] **Step 4: 跑测试，期望全 PASS**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -m pytest tests/test_ts_metrics.py -v
```

期望：6 passed。

- [ ] **Step 5: Commit**

```bash
git add ml_platform/app/core/ts_metrics.py ml_platform/tests/test_ts_metrics.py
git commit -m "feat(v3.4): ts_metrics module + 6 unit tests"
```

## Task 12: ARIMATrainer (TDD)

**Files:**
- Create: `ml_platform/tests/test_ts_trainer_arima.py`
- Modify: `ml_platform/app/core/ts_trainer.py`

- [ ] **Step 1: 写测试**

```python
# tests/test_ts_trainer_arima.py
from pathlib import Path
import numpy as np
import pandas as pd
import pytest
from app.core.ts_trainer import ARIMATrainer, TSMeta


@pytest.fixture
def sin_wave_df():
    n = 100
    ds = pd.date_range("2024-01-01", periods=n, freq="D")
    y = np.sin(np.arange(n) * 2 * np.pi / 30) + np.random.RandomState(0).randn(n) * 0.1
    return pd.DataFrame({"ds": ds, "y": y})


@pytest.fixture
def meta():
    return TSMeta(
        timestamp_col="ds", target_col="y", series_id_col=None,
        exogenous_cols=[], freq="D", horizon=10, lookback=30,
        interval_levels=[80, 95],
    )


def test_arima_fit_predict_shape(sin_wave_df, meta):
    t = ARIMATrainer()
    t.fit(sin_wave_df, meta, params={"p": 1, "d": 0, "q": 1, "seasonal_periods": 0})
    res = t.predict(horizon=10)
    assert res.mean.shape == (10,)
    assert res.intervals is not None
    assert 80 in res.intervals
    lo, hi = res.intervals[80]
    assert lo.shape == (10,) and hi.shape == (10,)
    assert np.all(lo <= hi)


def test_arima_save_load_roundtrip(sin_wave_df, meta, tmp_path):
    t = ARIMATrainer()
    t.fit(sin_wave_df, meta, params={"p": 1, "d": 0, "q": 1, "seasonal_periods": 0})
    p = tmp_path / "arima.joblib"
    t.save(p)
    t2 = ARIMATrainer.load(p)
    res1 = t.predict(horizon=5)
    res2 = t2.predict(horizon=5)
    np.testing.assert_allclose(res1.mean, res2.mean, rtol=1e-6)
```

- [ ] **Step 2: 跑测试，期望 fail**

```bash
docker exec ml_platform_backend python -m pytest tests/test_ts_trainer_arima.py -v
```

期望：ImportError on ARIMATrainer。

- [ ] **Step 3: 在 ts_trainer.py 末尾追加 ARIMATrainer**

```python
# Append to app/core/ts_trainer.py
import joblib


class ARIMATrainer(BaseTSTrainer):
    name = "arima"
    supports_intervals = True
    supports_exogenous = True

    def __init__(self):
        self._fitted = None
        self._meta: TSMeta | None = None

    def fit(self, train_df, meta, params):
        from statsmodels.tsa.arima.model import ARIMA
        order = (int(params.get("p", 1)), int(params.get("d", 1)), int(params.get("q", 1)))
        seasonal_p = int(params.get("seasonal_periods", 0))
        seasonal_order = (1, 1, 1, seasonal_p) if seasonal_p > 0 else (0, 0, 0, 0)
        y = train_df[meta.target_col].to_numpy()
        exog = None
        if meta.exogenous_cols:
            exog = train_df[meta.exogenous_cols].to_numpy()
        self._fitted = ARIMA(y, exog=exog, order=order, seasonal_order=seasonal_order).fit()
        self._meta = meta

    def predict(self, horizon, exog=None):
        fc = self._fitted.get_forecast(steps=horizon, exog=exog)
        mean = fc.predicted_mean
        intervals: dict[int, tuple] = {}
        for level in (self._meta.interval_levels if self._meta else [80, 95]):
            alpha = 1.0 - level / 100.0
            ci = fc.conf_int(alpha=alpha)
            # ci is ndarray-like (horizon, 2): col 0 lower, col 1 upper
            arr = ci if hasattr(ci, "shape") else ci.values
            intervals[level] = (arr[:, 0], arr[:, 1])
        return ForecastResult(mean=mean, intervals=intervals)

    def save(self, path):
        joblib.dump({"fitted": self._fitted, "meta": self._meta}, path)

    @classmethod
    def load(cls, path):
        data = joblib.load(path)
        t = cls()
        t._fitted = data["fitted"]
        t._meta = data["meta"]
        return t
```

- [ ] **Step 4: 跑测试，期望 PASS**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -m pytest tests/test_ts_trainer_arima.py -v
```

期望：2 passed。

- [ ] **Step 5: Commit**

```bash
git add ml_platform/app/core/ts_trainer.py ml_platform/tests/test_ts_trainer_arima.py
git commit -m "feat(v3.4): ARIMATrainer (statsmodels SARIMAX) + 2 tests"
```

## Task 13: ETSTrainer (TDD)

**Files:**
- Create: `ml_platform/tests/test_ts_trainer_ets.py`
- Modify: `ml_platform/app/core/ts_trainer.py`

- [ ] **Step 1: 写测试**

```python
# tests/test_ts_trainer_ets.py
import numpy as np
import pandas as pd
import pytest
from app.core.ts_trainer import ETSTrainer, TSMeta


@pytest.fixture
def trend_df():
    n = 60
    ds = pd.date_range("2024-01-01", periods=n, freq="D")
    y = np.arange(n, dtype=float) + np.random.RandomState(1).randn(n) * 0.5
    return pd.DataFrame({"ds": ds, "y": y})


@pytest.fixture
def meta():
    return TSMeta(
        timestamp_col="ds", target_col="y", series_id_col=None,
        exogenous_cols=[], freq="D", horizon=7, lookback=14,
        interval_levels=[80, 95],
    )


def test_ets_fits_trend(trend_df, meta):
    t = ETSTrainer()
    t.fit(trend_df, meta, params={"trend": "add", "seasonal": "none", "seasonal_periods": 0})
    res = t.predict(horizon=7)
    assert res.mean.shape == (7,)
    # mean should continue trend roughly (linearly increasing)
    assert res.mean[-1] > res.mean[0]


def test_ets_save_load(trend_df, meta, tmp_path):
    t = ETSTrainer()
    t.fit(trend_df, meta, params={"trend": "add", "seasonal": "none", "seasonal_periods": 0})
    p = tmp_path / "ets.joblib"
    t.save(p)
    t2 = ETSTrainer.load(p)
    np.testing.assert_allclose(t.predict(7).mean, t2.predict(7).mean, rtol=1e-6)
```

- [ ] **Step 2: 跑测试期望 fail**

```bash
docker exec ml_platform_backend python -m pytest tests/test_ts_trainer_ets.py -v
```

- [ ] **Step 3: 实现 ETSTrainer，追加到 ts_trainer.py**

```python
class ETSTrainer(BaseTSTrainer):
    name = "ets"
    supports_intervals = True
    supports_exogenous = False

    def __init__(self):
        self._fitted = None
        self._meta: TSMeta | None = None

    def fit(self, train_df, meta, params):
        from statsmodels.tsa.holtwinters import ExponentialSmoothing
        trend = params.get("trend", "add")
        seasonal = params.get("seasonal", "none")
        seasonal_p = int(params.get("seasonal_periods", 0))
        kwargs = {}
        if trend != "none":
            kwargs["trend"] = trend
        if seasonal != "none" and seasonal_p > 0:
            kwargs["seasonal"] = seasonal
            kwargs["seasonal_periods"] = seasonal_p
        y = train_df[meta.target_col].to_numpy()
        self._fitted = ExponentialSmoothing(y, **kwargs).fit()
        self._meta = meta

    def predict(self, horizon, exog=None):
        mean = self._fitted.forecast(steps=horizon)
        # ExponentialSmoothing has no built-in CI; approximate via residual std.
        resid = self._fitted.resid
        sigma = float(np.std(resid)) if len(resid) else 0.0
        intervals: dict[int, tuple] = {}
        from scipy.stats import norm
        for level in (self._meta.interval_levels if self._meta else [80, 95]):
            z = norm.ppf(0.5 + level / 200.0)
            half = z * sigma
            intervals[level] = (mean - half, mean + half)
        return ForecastResult(mean=np.asarray(mean), intervals=intervals)

    def save(self, path):
        joblib.dump({"fitted": self._fitted, "meta": self._meta}, path)

    @classmethod
    def load(cls, path):
        data = joblib.load(path)
        t = cls()
        t._fitted = data["fitted"]
        t._meta = data["meta"]
        return t
```

- [ ] **Step 4: 跑测试**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -m pytest tests/test_ts_trainer_ets.py -v
```

期望：2 passed。

- [ ] **Step 5: Commit**

```bash
git add ml_platform/app/core/ts_trainer.py ml_platform/tests/test_ts_trainer_ets.py
git commit -m "feat(v3.4): ETSTrainer (Holt-Winters) + 2 tests"
```

---

# M4 — LSTM + TCN forecaster

## Task 14: LSTMForecaster nn.Module + Trainer (TDD)

**Files:**
- Create: `ml_platform/app/core/dl_models/lstm_forecaster.py`
- Create: `ml_platform/tests/test_ts_trainer_lstm_forecaster.py`
- Modify: `ml_platform/app/core/ts_trainer.py`

- [ ] **Step 1: 写测试**

```python
# tests/test_ts_trainer_lstm_forecaster.py
import numpy as np
import pandas as pd
import pytest
from app.core.ts_trainer import LSTMForecasterTrainer, TSMeta


@pytest.fixture
def sin_df():
    n = 200
    ds = pd.date_range("2024-01-01", periods=n, freq="D")
    y = np.sin(np.arange(n) * 2 * np.pi / 20)
    return pd.DataFrame({"ds": ds, "y": y})


@pytest.fixture
def meta():
    return TSMeta(timestamp_col="ds", target_col="y", series_id_col=None,
                  exogenous_cols=[], freq="D", horizon=10, lookback=30,
                  interval_levels=[])


def test_lstm_forecaster_fit_predict(sin_df, meta):
    t = LSTMForecasterTrainer()
    t.fit(sin_df, meta, params={
        "hidden_size": 16, "num_layers": 1, "dropout": 0.0,
        "epochs": 5, "batch_size": 16, "learning_rate": 0.01,
    })
    res = t.predict(horizon=10)
    assert res.mean.shape == (10,)
    assert res.intervals is None  # baseline LSTM no intervals


def test_lstm_forecaster_save_load(sin_df, meta, tmp_path):
    t = LSTMForecasterTrainer()
    t.fit(sin_df, meta, params={
        "hidden_size": 8, "num_layers": 1, "dropout": 0.0,
        "epochs": 3, "batch_size": 16, "learning_rate": 0.01,
    })
    p = tmp_path / "lstm.pt"
    t.save(p)
    t2 = LSTMForecasterTrainer.load(p)
    np.testing.assert_allclose(t.predict(5).mean, t2.predict(5).mean, atol=1e-5)
```

- [ ] **Step 2: 跑测试期望 fail**

```bash
docker exec ml_platform_backend python -m pytest tests/test_ts_trainer_lstm_forecaster.py -v
```

- [ ] **Step 3: 写 nn.Module**

```python
# app/core/dl_models/lstm_forecaster.py
"""Direct multi-step LSTM forecaster — outputs `horizon` values from last hidden."""
from __future__ import annotations

import torch
import torch.nn as nn


class LSTMForecaster(nn.Module):
    def __init__(self, input_size: int, hidden_size: int, num_layers: int,
                 horizon: int, dropout: float = 0.1):
        super().__init__()
        self.lstm = nn.LSTM(
            input_size=input_size, hidden_size=hidden_size, num_layers=num_layers,
            batch_first=True,
            dropout=dropout if num_layers > 1 else 0.0,
        )
        self.head = nn.Linear(hidden_size, horizon)

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        # x: (batch, lookback, features) → (batch, horizon)
        out, _ = self.lstm(x)
        return self.head(out[:, -1, :])
```

- [ ] **Step 4: 写 LSTMForecasterTrainer，追加到 ts_trainer.py**

```python
class LSTMForecasterTrainer(BaseTSTrainer):
    name = "lstm_forecaster"
    supports_intervals = False
    supports_exogenous = True

    def __init__(self):
        self._model = None
        self._meta: TSMeta | None = None
        self._x_last: np.ndarray | None = None  # for autoregressive predict
        self._params: dict[str, Any] | None = None

    @staticmethod
    def _build_windows(values: np.ndarray, lookback: int, horizon: int):
        # values: (T, F)
        xs, ys = [], []
        for i in range(len(values) - lookback - horizon + 1):
            xs.append(values[i : i + lookback])
            ys.append(values[i + lookback : i + lookback + horizon, 0])  # target col 0
        return np.asarray(xs), np.asarray(ys)

    def fit(self, train_df, meta, params):
        import torch
        from torch.utils.data import DataLoader, TensorDataset
        from app.core.dl_models.lstm_forecaster import LSTMForecaster

        cols = [meta.target_col] + meta.exogenous_cols
        values = train_df[cols].to_numpy(dtype=np.float32)
        X, Y = self._build_windows(values, meta.lookback, meta.horizon)
        if len(X) == 0:
            raise ValueError(
                f"Not enough rows ({len(values)}) for lookback={meta.lookback}+horizon={meta.horizon}"
            )

        self._model = LSTMForecaster(
            input_size=len(cols),
            hidden_size=int(params["hidden_size"]),
            num_layers=int(params["num_layers"]),
            horizon=meta.horizon,
            dropout=float(params.get("dropout", 0.1)),
        )
        opt = torch.optim.Adam(self._model.parameters(), lr=float(params["learning_rate"]))
        loss_fn = torch.nn.MSELoss()
        ds = TensorDataset(torch.from_numpy(X), torch.from_numpy(Y))
        dl = DataLoader(ds, batch_size=int(params["batch_size"]), shuffle=True)

        self._model.train()
        for _ in range(int(params["epochs"])):
            for xb, yb in dl:
                opt.zero_grad()
                pred = self._model(xb)
                loss = loss_fn(pred, yb)
                loss.backward()
                opt.step()

        self._meta = meta
        self._params = params
        self._x_last = values[-meta.lookback:]

    def predict(self, horizon, exog=None):
        import torch
        self._model.eval()
        with torch.no_grad():
            x = torch.from_numpy(self._x_last).unsqueeze(0)  # (1, lookback, F)
            out = self._model(x).cpu().numpy()[0]  # (horizon,)
        return ForecastResult(mean=out[:horizon], intervals=None)

    def save(self, path):
        import torch
        torch.save({
            "state_dict": self._model.state_dict(),
            "meta": self._meta,
            "params": self._params,
            "x_last": self._x_last,
            "input_size": self._model.lstm.input_size,
        }, path)

    @classmethod
    def load(cls, path):
        import torch
        from app.core.dl_models.lstm_forecaster import LSTMForecaster
        data = torch.load(path, weights_only=False)
        t = cls()
        t._meta = data["meta"]
        t._params = data["params"]
        t._x_last = data["x_last"]
        t._model = LSTMForecaster(
            input_size=data["input_size"],
            hidden_size=int(t._params["hidden_size"]),
            num_layers=int(t._params["num_layers"]),
            horizon=t._meta.horizon,
            dropout=float(t._params.get("dropout", 0.1)),
        )
        t._model.load_state_dict(data["state_dict"])
        return t
```

- [ ] **Step 5: 跑测试**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -m pytest tests/test_ts_trainer_lstm_forecaster.py -v
```

期望：2 passed。

- [ ] **Step 6: Commit**

```bash
git add ml_platform/app/core/dl_models/lstm_forecaster.py ml_platform/app/core/ts_trainer.py ml_platform/tests/test_ts_trainer_lstm_forecaster.py
git commit -m "feat(v3.4): LSTMForecaster module + trainer + 2 tests"
```

## Task 15: TCNForecaster module + Trainer (TDD)

**Files:**
- Create: `ml_platform/app/core/dl_models/tcn_forecaster.py`
- Create: `ml_platform/tests/test_ts_trainer_tcn_forecaster.py`
- Modify: `ml_platform/app/core/ts_trainer.py`

- [ ] **Step 1: 写测试** (复制 LSTM 测试结构)

```python
# tests/test_ts_trainer_tcn_forecaster.py
import numpy as np
import pandas as pd
import pytest
from app.core.ts_trainer import TCNForecasterTrainer, TSMeta


@pytest.fixture
def sin_df():
    n = 200
    ds = pd.date_range("2024-01-01", periods=n, freq="D")
    y = np.sin(np.arange(n) * 2 * np.pi / 20)
    return pd.DataFrame({"ds": ds, "y": y})


@pytest.fixture
def meta():
    return TSMeta(timestamp_col="ds", target_col="y", series_id_col=None,
                  exogenous_cols=[], freq="D", horizon=10, lookback=30,
                  interval_levels=[])


def test_tcn_forecaster_fit_predict(sin_df, meta):
    t = TCNForecasterTrainer()
    t.fit(sin_df, meta, params={
        "channels": 16, "kernel_size": 3, "num_layers": 2, "dropout": 0.0,
        "epochs": 5, "batch_size": 16, "learning_rate": 0.01,
    })
    res = t.predict(horizon=10)
    assert res.mean.shape == (10,)
    assert res.intervals is None


def test_tcn_save_load(sin_df, meta, tmp_path):
    t = TCNForecasterTrainer()
    t.fit(sin_df, meta, params={
        "channels": 8, "kernel_size": 3, "num_layers": 2, "dropout": 0.0,
        "epochs": 3, "batch_size": 16, "learning_rate": 0.01,
    })
    p = tmp_path / "tcn.pt"
    t.save(p)
    t2 = TCNForecasterTrainer.load(p)
    np.testing.assert_allclose(t.predict(5).mean, t2.predict(5).mean, atol=1e-5)
```

- [ ] **Step 2: 跑测试 fail**

```bash
docker exec ml_platform_backend python -m pytest tests/test_ts_trainer_tcn_forecaster.py -v
```

- [ ] **Step 3: 写 TCN 模块**

```python
# app/core/dl_models/tcn_forecaster.py
"""Causal-conv TCN forecaster — direct multi-step output."""
from __future__ import annotations

import torch
import torch.nn as nn


class CausalConv1d(nn.Conv1d):
    def __init__(self, in_ch, out_ch, kernel_size, dilation):
        self._pad = (kernel_size - 1) * dilation
        super().__init__(in_ch, out_ch, kernel_size, padding=self._pad, dilation=dilation)

    def forward(self, x):
        out = super().forward(x)
        return out[:, :, : -self._pad] if self._pad > 0 else out


class TCNBlock(nn.Module):
    def __init__(self, in_ch, out_ch, kernel_size, dilation, dropout):
        super().__init__()
        self.conv1 = CausalConv1d(in_ch, out_ch, kernel_size, dilation)
        self.conv2 = CausalConv1d(out_ch, out_ch, kernel_size, dilation)
        self.relu = nn.ReLU()
        self.drop = nn.Dropout(dropout)
        self.skip = nn.Conv1d(in_ch, out_ch, 1) if in_ch != out_ch else nn.Identity()

    def forward(self, x):
        out = self.drop(self.relu(self.conv1(x)))
        out = self.drop(self.relu(self.conv2(out)))
        return self.relu(out + self.skip(x))


class TCNForecaster(nn.Module):
    def __init__(self, input_size: int, channels: int, kernel_size: int,
                 num_layers: int, horizon: int, dropout: float = 0.1):
        super().__init__()
        layers = []
        prev = input_size
        for i in range(num_layers):
            layers.append(TCNBlock(prev, channels, kernel_size, dilation=2 ** i, dropout=dropout))
            prev = channels
        self.tcn = nn.Sequential(*layers)
        self.head = nn.Linear(channels, horizon)

    def forward(self, x):
        # x: (batch, lookback, features) → permute to (batch, features, lookback)
        h = self.tcn(x.transpose(1, 2))
        return self.head(h[:, :, -1])
```

- [ ] **Step 4: 写 TCNForecasterTrainer (复制 LSTM 模式，替换 nn.Module)**

```python
# Append to ts_trainer.py

class TCNForecasterTrainer(BaseTSTrainer):
    name = "tcn_forecaster"
    supports_intervals = False
    supports_exogenous = True

    def __init__(self):
        self._model = None
        self._meta = None
        self._x_last = None
        self._params = None

    _build_windows = staticmethod(LSTMForecasterTrainer._build_windows.__func__)

    def fit(self, train_df, meta, params):
        import torch
        from torch.utils.data import DataLoader, TensorDataset
        from app.core.dl_models.tcn_forecaster import TCNForecaster

        cols = [meta.target_col] + meta.exogenous_cols
        values = train_df[cols].to_numpy(dtype=np.float32)
        X, Y = self._build_windows(values, meta.lookback, meta.horizon)
        if len(X) == 0:
            raise ValueError(
                f"Not enough rows ({len(values)}) for lookback={meta.lookback}+horizon={meta.horizon}"
            )

        self._model = TCNForecaster(
            input_size=len(cols),
            channels=int(params["channels"]),
            kernel_size=int(params["kernel_size"]),
            num_layers=int(params["num_layers"]),
            horizon=meta.horizon,
            dropout=float(params.get("dropout", 0.1)),
        )
        opt = torch.optim.Adam(self._model.parameters(), lr=float(params["learning_rate"]))
        loss_fn = torch.nn.MSELoss()
        ds = TensorDataset(torch.from_numpy(X), torch.from_numpy(Y))
        dl = DataLoader(ds, batch_size=int(params["batch_size"]), shuffle=True)

        self._model.train()
        for _ in range(int(params["epochs"])):
            for xb, yb in dl:
                opt.zero_grad()
                pred = self._model(xb)
                loss = loss_fn(pred, yb)
                loss.backward()
                opt.step()

        self._meta = meta
        self._params = params
        self._x_last = values[-meta.lookback:]

    def predict(self, horizon, exog=None):
        import torch
        self._model.eval()
        with torch.no_grad():
            x = torch.from_numpy(self._x_last).unsqueeze(0)
            out = self._model(x).cpu().numpy()[0]
        return ForecastResult(mean=out[:horizon], intervals=None)

    def save(self, path):
        import torch
        torch.save({
            "state_dict": self._model.state_dict(),
            "meta": self._meta,
            "params": self._params,
            "x_last": self._x_last,
            "input_size": self._model.tcn[0].conv1.in_channels,
        }, path)

    @classmethod
    def load(cls, path):
        import torch
        from app.core.dl_models.tcn_forecaster import TCNForecaster
        data = torch.load(path, weights_only=False)
        t = cls()
        t._meta = data["meta"]
        t._params = data["params"]
        t._x_last = data["x_last"]
        t._model = TCNForecaster(
            input_size=data["input_size"],
            channels=int(t._params["channels"]),
            kernel_size=int(t._params["kernel_size"]),
            num_layers=int(t._params["num_layers"]),
            horizon=t._meta.horizon,
            dropout=float(t._params.get("dropout", 0.1)),
        )
        t._model.load_state_dict(data["state_dict"])
        return t
```

- [ ] **Step 5: 跑测试**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -m pytest tests/test_ts_trainer_tcn_forecaster.py -v
```

期望：2 passed。

- [ ] **Step 6: Commit**

```bash
git add ml_platform/app/core/dl_models/tcn_forecaster.py ml_platform/app/core/ts_trainer.py ml_platform/tests/test_ts_trainer_tcn_forecaster.py
git commit -m "feat(v3.4): TCNForecaster module + trainer + 2 tests"
```

---

# M5 — TimesFM adapter

## Task 16: TimesFMAdapter (复用现有 venv subprocess)

**Files:**
- Create: `ml_platform/tests/test_ts_trainer_timesfm.py`
- Modify: `ml_platform/app/core/ts_trainer.py`

- [ ] **Step 1: 写测试**

```python
# tests/test_ts_trainer_timesfm.py
import json
from pathlib import Path
import numpy as np
import pandas as pd
import pytest
from app.core.ts_trainer import TimesFMAdapter, TSMeta


@pytest.fixture
def df():
    n = 100
    return pd.DataFrame({
        "ds": pd.date_range("2024-01-01", periods=n, freq="D"),
        "y":  np.sin(np.arange(n) / 10),
    })


@pytest.fixture
def meta():
    return TSMeta(timestamp_col="ds", target_col="y", series_id_col=None,
                  exogenous_cols=[], freq="D", horizon=14, lookback=64,
                  interval_levels=[])


def test_timesfm_save_load_marker(df, meta, tmp_path):
    """Even when venv missing, save/load round-trip should work via marker.json."""
    t = TimesFMAdapter()
    t.fit(df, meta, params={"context_len": 64})  # fit is a no-op
    p = tmp_path / "timesfm.json"
    t.save(p)
    assert p.exists()
    payload = json.loads(p.read_text())
    assert payload["model"] == "timesfm_1"

    t2 = TimesFMAdapter.load(p)
    assert t2._meta.horizon == 14
```

- [ ] **Step 2: 跑测试 fail**

```bash
docker exec ml_platform_backend python -m pytest tests/test_ts_trainer_timesfm.py -v
```

- [ ] **Step 3: 实现 adapter，追加到 ts_trainer.py**

```python
class TimesFMAdapter(BaseTSTrainer):
    """Wraps timesfm_service subprocess call as a BaseTSTrainer.

    fit is a no-op (foundation model; nothing to train).
    predict shells out to timesfm_env Python 3.10 venv.
    save/load persist only marker.json — actual model weights live in venv.
    """
    name = "timesfm_1"
    supports_intervals = False
    supports_exogenous = False

    def __init__(self):
        self._meta: TSMeta | None = None
        self._train_values: np.ndarray | None = None  # used as context for forecast
        self._params: dict[str, Any] | None = None

    def fit(self, train_df, meta, params):
        # no-op for foundation model; just stash context for predict()
        self._meta = meta
        self._params = params or {"context_len": 512}
        self._train_values = train_df[meta.target_col].to_numpy(dtype=np.float64)

    def predict(self, horizon, exog=None):
        from app.services.timesfm_service import _run_forecast_subprocess
        ctx = self._train_values
        if self._params and "context_len" in self._params:
            ctx_len = int(self._params["context_len"])
            ctx = ctx[-ctx_len:]
        result = _run_forecast_subprocess({
            "history": ctx.tolist(),
            "horizon": int(horizon),
            "freq": self._meta.freq if self._meta else "D",
        })
        if "forecast" not in result:
            raise RuntimeError(f"TimesFM subprocess returned no forecast: {result}")
        return ForecastResult(mean=np.asarray(result["forecast"], dtype=float), intervals=None)

    def save(self, path):
        import json
        path.write_text(json.dumps({
            "model": "timesfm_1",
            "meta": {
                "timestamp_col": self._meta.timestamp_col,
                "target_col": self._meta.target_col,
                "freq": self._meta.freq,
                "horizon": self._meta.horizon,
            } if self._meta else None,
            "params": self._params,
            "train_values": self._train_values.tolist() if self._train_values is not None else None,
        }))

    @classmethod
    def load(cls, path):
        import json
        data = json.loads(path.read_text())
        t = cls()
        meta_d = data.get("meta") or {}
        t._meta = TSMeta(
            timestamp_col=meta_d.get("timestamp_col", "ds"),
            target_col=meta_d.get("target_col", "y"),
            series_id_col=None, exogenous_cols=[],
            freq=meta_d.get("freq", "D"),
            horizon=int(meta_d.get("horizon", 1)),
            lookback=64, interval_levels=[],
        )
        t._params = data.get("params") or {}
        tv = data.get("train_values")
        t._train_values = np.asarray(tv, dtype=np.float64) if tv is not None else None
        return t
```

- [ ] **Step 4: 跑测试**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -m pytest tests/test_ts_trainer_timesfm.py -v
```

期望：1 passed。

- [ ] **Step 5: Commit**

```bash
git add ml_platform/app/core/ts_trainer.py ml_platform/tests/test_ts_trainer_timesfm.py
git commit -m "feat(v3.4): TimesFMAdapter wraps existing 3.10 venv subprocess"
```

## Task 17: 在 ts_trainer.py 加 TRAINER_REGISTRY 工厂

**Files:**
- Modify: `ml_platform/app/core/ts_trainer.py`

- [ ] **Step 1: 在文件末尾追加**

```python
TS_TRAINER_REGISTRY: dict[str, type[BaseTSTrainer]] = {
    "arima": ARIMATrainer,
    "ets": ETSTrainer,
    "lstm_forecaster": LSTMForecasterTrainer,
    "tcn_forecaster": TCNForecasterTrainer,
    "timesfm_1": TimesFMAdapter,
}


def get_ts_trainer(token: str) -> BaseTSTrainer:
    cls = TS_TRAINER_REGISTRY.get(token)
    if cls is None:
        raise KeyError(f"Unknown ts trainer token: {token!r}. Known: {list(TS_TRAINER_REGISTRY)}")
    return cls()
```

- [ ] **Step 2: 校验**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -c "from app.core.ts_trainer import get_ts_trainer; t = get_ts_trainer('arima'); print(type(t).__name__)"
```

期望：`ARIMATrainer`。

- [ ] **Step 3: Commit**

```bash
git add ml_platform/app/core/ts_trainer.py
git commit -m "feat(v3.4): TS_TRAINER_REGISTRY factory for token→trainer"
```

---

# M6 — ts_service 完整实现 + 接 V3 调度

## Task 18: 把 ts_service.run_ts_executor 从 stub 实现到完整版（TDD）

**Files:**
- Modify: `ml_platform/app/services/ts_service.py`
- Create: `ml_platform/tests/test_ts_service_executor.py`

- [ ] **Step 1: 写集成测试（用 ARIMA 跑一轮，验证整个 executor 链）**

```python
# tests/test_ts_service_executor.py
import asyncio
from pathlib import Path
import numpy as np
import pandas as pd
import pytest
from sqlalchemy import select

from app.models.database import (
    async_session_factory, Dataset, ModelingTask, PlatformExperiment,
    ExperimentRun, PlatformTask, TrainingPlan,
)
from app.services.ts_service import run_ts_executor
from app.config import get_settings


@pytest.mark.asyncio
async def test_ts_executor_arima_end_to_end(tmp_path):
    """Build a fake dataset + plan + run, dispatch run_ts_executor, assert metrics."""
    # 1. Write fake CSV
    n = 120
    csv_path = tmp_path / "sin.csv"
    pd.DataFrame({
        "ds": pd.date_range("2024-01-01", periods=n, freq="D"),
        "y": np.sin(np.arange(n) / 10),
    }).to_csv(csv_path, index=False)

    settings = get_settings()
    # 2. Create db rows
    async with async_session_factory() as session:
        ds = Dataset(name="sin.csv", file_path=str(csv_path),
                     row_count=n, column_count=2, columns_info={})
        session.add(ds)
        await session.flush()

        plan = TrainingPlan(
            dataset_id=ds.id, name="ts-test", task_type="forecasting",
            model_family="ts", strategy_type="baseline",
            payload={
                "model_tokens": ["arima"],
                "time_series": {
                    "timestamp_col": "ds", "target_col": "y", "series_id_col": None,
                    "exogenous_cols": [], "freq": "D",
                    "horizon": 7, "lookback": 14,
                    "validation": {"method": "holdout", "test_size": 7, "step": 1},
                    "interval_levels": [80, 95],
                },
            },
        )
        session.add(plan)
        await session.flush()

        mtask = ModelingTask(name="t", dataset_id=ds.id, task_type="forecasting",
                             training_plan_id=plan.id)
        session.add(mtask)
        await session.flush()

        exp = PlatformExperiment(modeling_task_id=mtask.id, name="e",
                                 strategy_type="baseline", model_family="ts",
                                 task_type="forecasting", status="QUEUED")
        session.add(exp)
        await session.flush()

        pt = PlatformTask(kind="ts_train", status="QUEUED",
                          payload_ref=f"train:{exp.id}")
        session.add(pt)
        await session.flush()

        run = ExperimentRun(experiment_id=exp.id, platform_task_id=pt.id,
                            model_type="arima", task_type="forecasting",
                            params={"p": 1, "d": 0, "q": 1},
                            status="QUEUED")
        session.add(run)
        await session.commit()
        run_id = run.id
        pt_id = pt.id

    # 3. Run executor
    result = await run_ts_executor(domain_id=run_id, platform_task_id=pt_id)

    # 4. Assert metrics
    assert "metrics" in result
    m = result["metrics"]
    for k in ("mae", "rmse", "mape", "smape"):
        assert k in m and m[k] is not None
```

- [ ] **Step 2: 跑测试 fail（stub raises NotImplementedError）**

```bash
docker exec ml_platform_backend python -m pytest tests/test_ts_service_executor.py -v
```

- [ ] **Step 3: 实现 ts_service**

替换 `ml_platform/app/services/ts_service.py` 的全部内容：

```python
"""TS Service — V3 executor for ts family training runs."""
from __future__ import annotations

import logging
from pathlib import Path
from typing import Any

import numpy as np
import pandas as pd
from sqlalchemy import select

from app.config import get_settings
from app.core.ts_metrics import mae, rmse, mape, smape, mase, coverage
from app.core.ts_trainer import TSMeta, get_ts_trainer
from app.models.database import (
    async_session_factory, Dataset, ExperimentRun, PlatformExperiment,
    PlatformTask, TrainingPlan,
)
from app.scheduler.executors import register_executor

logger = logging.getLogger(__name__)


# ---------------------------------------------------------------------------
# Helpers
# ---------------------------------------------------------------------------

def _slice_holdout(df: pd.DataFrame, target_col: str, test_size: int):
    if len(df) <= test_size:
        raise ValueError(f"Dataset rows ({len(df)}) <= test_size ({test_size})")
    train = df.iloc[:-test_size].copy()
    test = df.iloc[-test_size:].copy()
    return train, test


def _build_meta(ts_payload: dict) -> TSMeta:
    return TSMeta(
        timestamp_col=ts_payload["timestamp_col"],
        target_col=ts_payload["target_col"],
        series_id_col=ts_payload.get("series_id_col"),
        exogenous_cols=ts_payload.get("exogenous_cols", []),
        freq=ts_payload["freq"],
        horizon=int(ts_payload["horizon"]),
        lookback=int(ts_payload.get("lookback", 28)),
        interval_levels=ts_payload.get("interval_levels", [80, 95]),
    )


def _resolve_extension(token: str) -> str:
    if token in ("arima", "ets"):
        return ".joblib"
    if token in ("lstm_forecaster", "tcn_forecaster"):
        return ".pt"
    if token == "timesfm_1":
        return ".json"
    raise ValueError(f"unknown ts token: {token}")


# ---------------------------------------------------------------------------
# Executor
# ---------------------------------------------------------------------------

async def run_ts_executor(domain_id: str, platform_task_id: str) -> dict[str, Any]:
    """V3 executor for ts family. domain_id = ExperimentRun.id."""
    settings = get_settings()

    async with async_session_factory() as session:
        run = (await session.execute(
            select(ExperimentRun).where(ExperimentRun.id == domain_id)
        )).scalar_one()

        exp = (await session.execute(
            select(PlatformExperiment).where(PlatformExperiment.id == run.experiment_id)
        )).scalar_one()

        plan = (await session.execute(
            select(TrainingPlan).where(TrainingPlan.id == exp.training_plan_id)
            if exp.training_plan_id else
            select(TrainingPlan).where(TrainingPlan.dataset_id == exp.dataset_id)
        )).scalars().first()

        # Fallback: lookup ModelingTask → snapshot
        if plan is None and exp.modeling_task_id:
            from app.models.database import ModelingTask
            mtask = (await session.execute(
                select(ModelingTask).where(ModelingTask.id == exp.modeling_task_id)
            )).scalar_one()
            plan = (await session.execute(
                select(TrainingPlan).where(TrainingPlan.id == mtask.training_plan_id)
            )).scalar_one()

        ds = (await session.execute(
            select(Dataset).where(Dataset.id == plan.dataset_id)
        )).scalar_one()

    # Load data
    ts_payload = plan.payload["time_series"]
    meta = _build_meta(ts_payload)
    df = pd.read_csv(ds.file_path)
    df = df.sort_values([meta.timestamp_col]).reset_index(drop=True)

    # Holdout split (only method we ship in M6)
    val_method = ts_payload["validation"]["method"]
    if val_method != "holdout":
        raise NotImplementedError(f"validation.method={val_method!r} not in M6 scope")
    train_df, test_df = _slice_holdout(df, meta.target_col, int(ts_payload["validation"]["test_size"]))

    # Train
    trainer = get_ts_trainer(run.model_type)
    trainer.fit(train_df, meta, params=run.params or {})

    # Predict
    horizon = len(test_df)
    res = trainer.predict(horizon=horizon)
    y_true = test_df[meta.target_col].to_numpy(dtype=float)
    y_pred = np.asarray(res.mean, dtype=float)
    train_y = train_df[meta.target_col].to_numpy(dtype=float)

    metrics: dict[str, float] = {
        "mae":   mae(y_true, y_pred),
        "rmse":  rmse(y_true, y_pred),
        "mape":  mape(y_true, y_pred),
        "smape": smape(y_true, y_pred),
        "mase":  mase(y_true, y_pred, train_y, season=1),
    }
    if res.intervals:
        for level, (lo, hi) in res.intervals.items():
            metrics[f"coverage_{level}"] = coverage(y_true, lo, hi)

    # Persist
    ext = _resolve_extension(run.model_type)
    model_path = settings.storage_models / f"{run.id}{ext}"
    settings.storage_models.mkdir(parents=True, exist_ok=True)
    trainer.save(model_path)

    logger.info("TS run %s done: %s", run.id, metrics)
    return {
        "metrics": metrics,
        "artifacts": {
            "model_path": str(model_path.relative_to(settings.storage_models.parent)),
            "horizon": horizon,
        },
    }


register_executor("ts_train", run_ts_executor)
```

- [ ] **Step 4: 跑测试 expect PASS**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -m pytest tests/test_ts_service_executor.py -v -s 2>&1 | tail -40
```

期望：1 passed。如有 fail，看断言 trace 调整 db 字段名（可能有 ModelingTask 字段名差异）。

- [ ] **Step 5: Commit**

```bash
git add ml_platform/app/services/ts_service.py ml_platform/tests/test_ts_service_executor.py
git commit -m "feat(v3.4): ts_service.run_ts_executor full impl + e2e test"
```

## Task 19: 注册 ts_v3_router

**Files:**
- Create: `ml_platform/app/api/routes/ts_v3.py`
- Modify: `ml_platform/app/main.py`

- [ ] **Step 1: 写最小 router**

```python
# app/api/routes/ts_v3.py
"""V3-style TS endpoints — model registry + forecast playback for finished runs."""
from __future__ import annotations

from fastapi import APIRouter, HTTPException

from app.core.ts_registry import list_available_models, get_ts_model_spec

router = APIRouter(prefix="/v3/ts", tags=["TS V3"])  # /api/v3/ts/... — sits alongside /api/v3/tasks, avoids /api/ts/{task_id} catch-all


@router.get("/models")
def list_ts_models():
    """List registered ts family models with availability flag."""
    return {"items": list_available_models()}


@router.get("/models/{model_id}")
def get_ts_model(model_id: str):
    spec = get_ts_model_spec(model_id)
    if spec is None:
        raise HTTPException(404, f"ts model not found: {model_id}")
    return spec
```

- [ ] **Step 2: 在 main.py 注册**

找到 `app.include_router(ts_router, prefix="/api")` 这一行（已有），在其下方追加：

```python
    from app.api.routes.ts_v3 import router as ts_v3_router
    app.include_router(ts_v3_router, prefix="/api")  # → /api/v3/ts/...
```

- [ ] **Step 3: 校验**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
curl -s http://localhost:8000/api/v3/ts/models | python3 -c "import sys,json; print(len(json.load(sys.stdin)['items']))"
```

期望：`5`。

- [ ] **Step 4: Commit**

```bash
git add ml_platform/app/api/routes/ts_v3.py ml_platform/app/main.py
git commit -m "feat(v3.4): /api/v3/ts/models endpoint"
```

## Task 20: platform_task_detail_service forecasting 分支

**Files:**
- Modify: `ml_platform/app/services/platform_task_detail_service.py`

- [ ] **Step 1: 找 metrics/charts 装配处**

```bash
grep -n "task_type\|task_kind\|build_run_detail\|metrics\|chart" ml_platform/app/services/platform_task_detail_service.py | head -20
```

- [ ] **Step 2: 在装配 chart_specs 处加 forecasting 分支**

找类似：

```python
    if task_kind == "regression":
        chart_specs = [...regression list...]
    else:
        chart_specs = [...classification list...]
```

改为：

```python
    if task_kind == "forecasting":
        chart_specs = [
            {"id": "forecast_line", "label": "预测对比", "endpoint": f"/api/viz/{run_id}/forecast_line"},
            {"id": "residual",       "label": "残差",     "endpoint": f"/api/viz/{run_id}/residual"},
        ]
    elif task_kind == "regression":
        chart_specs = [...]
    else:
        chart_specs = [...]
```

具体行号 grep 确认。

- [ ] **Step 3: 校验**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -c "from app.services.platform_task_detail_service import build_run_detail; print('OK')"
```

- [ ] **Step 4: Commit**

```bash
git add ml_platform/app/services/platform_task_detail_service.py
git commit -m "feat(v3.4): platform_task_detail forecasting chart specs"
```

## Task 21: visualization.py 加 forecast_line / residual 端点

**Files:**
- Modify: `ml_platform/app/api/routes/visualization.py`

- [ ] **Step 1: 在文件末尾加两个 GET 端点**

```python
# app/api/routes/visualization.py — append at end

from app.services.resolver import resolve_task_and_dataset, is_forecaster
from app.core.ts_trainer import get_ts_trainer
from app.config import get_settings
import pandas as pd


@router.get("/{task_id}/forecast_line")
async def forecast_line(task_id: str, session: AsyncSession = Depends(get_session)):
    facade = await resolve_task_and_dataset(session, task_id)
    if not is_forecaster(facade.model_type):
        raise HTTPException(400, "Not a forecasting task")
    settings = get_settings()
    # Find model file (multi-extension)
    from app.services.resolver import _find_model_file
    model_file, ext = _find_model_file(settings.storage_models, facade.task_id)
    if model_file is None:
        raise HTTPException(404, "Model file missing")
    trainer = get_ts_trainer(facade.model_type)
    trainer = trainer.__class__.load(model_file)

    df = pd.read_csv(facade.dataset_path)
    # last `horizon` rows are the holdout test; previous are train
    horizon = trainer._meta.horizon if trainer._meta else 14
    df = df.sort_values([trainer._meta.timestamp_col]).reset_index(drop=True)
    test = df.iloc[-horizon:]
    res = trainer.predict(horizon=horizon)
    return {
        "timestamps": test[trainer._meta.timestamp_col].astype(str).tolist(),
        "actual":     test[trainer._meta.target_col].tolist(),
        "predicted":  res.mean.tolist(),
        "intervals":  {str(k): {"low": lo.tolist(), "high": hi.tolist()}
                       for k, (lo, hi) in (res.intervals or {}).items()},
    }


@router.get("/{task_id}/residual")
async def residual(task_id: str, session: AsyncSession = Depends(get_session)):
    data = await forecast_line(task_id, session)  # reuse loader
    actual = data["actual"]
    pred = data["predicted"]
    return {
        "timestamps": data["timestamps"],
        "residuals": [a - p for a, p in zip(actual, pred)],
    }
```

- [ ] **Step 2: 校验**

```bash
docker compose -f docker/docker-compose.yml restart backend && sleep 10
docker exec ml_platform_backend python -c "from app.api.routes.visualization import router; print(any(r.path.endswith('/forecast_line') for r in router.routes))"
```

期望：`True`。

- [ ] **Step 3: Commit**

```bash
git add ml_platform/app/api/routes/visualization.py
git commit -m "feat(v3.4): forecast_line + residual viz endpoints"
```

---

# M7 — 前端

## Task 22: TimeSeriesPlanForm + ColumnSelector + ValidationConfig

**Files:**
- Create: `ml_platform_web/src/components/forecasting/TimeSeriesPlanForm.jsx`
- Create: `ml_platform_web/src/components/forecasting/ColumnSelector.jsx`
- Create: `ml_platform_web/src/components/forecasting/ValidationConfig.jsx`

- [ ] **Step 1: ColumnSelector**

```jsx
// ml_platform_web/src/components/forecasting/ColumnSelector.jsx
import { Form, Select } from 'antd'

export default function ColumnSelector({ columns = [], value, onChange }) {
  const items = columns.map(c => ({ label: c, value: c }))
  return (
    <>
      <Form.Item label="时间列" required>
        <Select options={items}
                value={value?.timestamp_col}
                onChange={v => onChange({ ...value, timestamp_col: v })} />
      </Form.Item>
      <Form.Item label="目标列" required>
        <Select options={items}
                value={value?.target_col}
                onChange={v => onChange({ ...value, target_col: v })} />
      </Form.Item>
      <Form.Item label="分组列 (单序列留空)">
        <Select options={items} allowClear
                value={value?.series_id_col}
                onChange={v => onChange({ ...value, series_id_col: v || null })} />
      </Form.Item>
      <Form.Item label="外生特征列">
        <Select mode="multiple" options={items}
                value={value?.exogenous_cols || []}
                onChange={v => onChange({ ...value, exogenous_cols: v })} />
      </Form.Item>
    </>
  )
}
```

- [ ] **Step 2: ValidationConfig**

```jsx
// ml_platform_web/src/components/forecasting/ValidationConfig.jsx
import { Form, Select, InputNumber } from 'antd'

export default function ValidationConfig({ value, onChange }) {
  const v = value || { method: 'holdout', test_size: 14, step: 1 }
  return (
    <>
      <Form.Item label="验证方法" required>
        <Select value={v.method}
                onChange={m => onChange({ ...v, method: m })}
                options={[
                  { value: 'holdout', label: 'Holdout（留出）' },
                  { value: 'rolling_origin', label: 'Rolling-Origin' },
                  { value: 'expanding_window', label: 'Expanding Window' },
                ]} />
      </Form.Item>
      <Form.Item label="验证步数" required>
        <InputNumber min={1} max={1000} value={v.test_size}
                     onChange={n => onChange({ ...v, test_size: n })} />
      </Form.Item>
      {v.method !== 'holdout' && (
        <Form.Item label="step">
          <InputNumber min={1} value={v.step}
                       onChange={n => onChange({ ...v, step: n })} />
        </Form.Item>
      )}
    </>
  )
}
```

- [ ] **Step 3: TimeSeriesPlanForm**

```jsx
// ml_platform_web/src/components/forecasting/TimeSeriesPlanForm.jsx
import { Form, Select, InputNumber } from 'antd'
import ColumnSelector from './ColumnSelector'
import ValidationConfig from './ValidationConfig'

const FREQS = [
  { value: 'D', label: 'D — 日' },
  { value: 'H', label: 'H — 小时' },
  { value: '15min', label: '15min — 15分钟' },
  { value: 'W', label: 'W — 周' },
  { value: 'M', label: 'M — 月' },
]

export default function TimeSeriesPlanForm({ columns = [], value, onChange }) {
  const v = value || {
    timestamp_col: null, target_col: null, series_id_col: null,
    exogenous_cols: [], freq: 'D', horizon: 14, lookback: 28,
    validation: { method: 'holdout', test_size: 14, step: 1 },
    interval_levels: [80, 95],
  }
  const set = (patch) => onChange({ ...v, ...patch })
  return (
    <>
      <ColumnSelector columns={columns} value={v}
                      onChange={(cols) => set(cols)} />
      <Form.Item label="频率" required>
        <Select options={FREQS} value={v.freq}
                onChange={f => set({ freq: f })} />
      </Form.Item>
      <Form.Item label="预测步长 (horizon)" required>
        <InputNumber min={1} max={10000} value={v.horizon}
                     onChange={n => set({ horizon: n })} />
      </Form.Item>
      <Form.Item label="历史窗口 (lookback)">
        <InputNumber min={1} max={10000} value={v.lookback}
                     onChange={n => set({ lookback: n })} />
      </Form.Item>
      <ValidationConfig value={v.validation}
                        onChange={val => set({ validation: val })} />
    </>
  )
}
```

- [ ] **Step 4: 验证 build**

```bash
cd ml_platform_web && npm run build 2>&1 | tail -10
```

期望 build 成功。

- [ ] **Step 5: Commit**

```bash
git add ml_platform_web/src/components/forecasting/
git commit -m "feat(v3.4): TimeSeriesPlanForm + ColumnSelector + ValidationConfig"
```

## Task 23: ForecastChart + ForecastMetrics

**Files:**
- Create: `ml_platform_web/src/components/forecasting/ForecastChart.jsx`
- Create: `ml_platform_web/src/components/forecasting/ForecastMetrics.jsx`

- [ ] **Step 1: ForecastChart (ECharts)**

```jsx
// ml_platform_web/src/components/forecasting/ForecastChart.jsx
import { useEffect, useRef } from 'react'
import * as echarts from 'echarts'

export default function ForecastChart({ data }) {
  const ref = useRef(null)
  useEffect(() => {
    if (!ref.current || !data) return
    const chart = echarts.init(ref.current)
    const series = [
      { name: '真值', type: 'line', data: data.actual, lineStyle: { width: 2 } },
      { name: '预测', type: 'line', data: data.predicted, lineStyle: { type: 'dashed' } },
    ]
    if (data.intervals?.['80']) {
      series.push({
        name: '80%区间', type: 'line', data: data.intervals['80'].high,
        lineStyle: { opacity: 0 }, areaStyle: { opacity: 0.2 }, stack: 'band',
      })
    }
    chart.setOption({
      tooltip: { trigger: 'axis' },
      legend: {},
      xAxis: { type: 'category', data: data.timestamps },
      yAxis: { type: 'value' },
      series,
    })
    return () => chart.dispose()
  }, [data])
  return <div ref={ref} style={{ width: '100%', height: 360 }} />
}
```

- [ ] **Step 2: ForecastMetrics 卡片**

```jsx
// ml_platform_web/src/components/forecasting/ForecastMetrics.jsx
import { Card, Statistic, Row, Col } from 'antd'

const KEYS = [
  { key: 'mae',   label: 'MAE',         precision: 4 },
  { key: 'rmse',  label: 'RMSE',        precision: 4 },
  { key: 'mape',  label: 'MAPE %',      precision: 2 },
  { key: 'smape', label: 'sMAPE %',     precision: 2 },
  { key: 'mase',  label: 'MASE',        precision: 4 },
  { key: 'coverage_80', label: '80% 覆盖率', precision: 3 },
  { key: 'coverage_95', label: '95% 覆盖率', precision: 3 },
]

export default function ForecastMetrics({ metrics = {} }) {
  return (
    <Row gutter={16}>
      {KEYS.filter(k => metrics[k.key] !== undefined).map(k => (
        <Col span={6} key={k.key}>
          <Card><Statistic title={k.label} value={metrics[k.key]} precision={k.precision} /></Card>
        </Col>
      ))}
    </Row>
  )
}
```

- [ ] **Step 3: build 检查**

```bash
cd ml_platform_web && npm run build 2>&1 | tail -10
```

- [ ] **Step 4: Commit**

```bash
git add ml_platform_web/src/components/forecasting/ForecastChart.jsx ml_platform_web/src/components/forecasting/ForecastMetrics.jsx
git commit -m "feat(v3.4): ForecastChart (ECharts) + ForecastMetrics cards"
```

## Task 24: ModelingTaskDetail 加 forecasting 任务类型

**Files:**
- Modify: `ml_platform_web/src/pages/ModelingTaskDetail.jsx`

- [ ] **Step 1: grep task_type 选项**

```bash
grep -n "task_type\|classification\|regression" ml_platform_web/src/pages/ModelingTaskDetail.jsx | head
```

- [ ] **Step 2: 在 task_type Select options 加入 forecasting**

找到类似：

```jsx
<Select options={[
  { value: 'classification', label: '分类' },
  { value: 'regression',     label: '回归' },
]} />
```

替换为：

```jsx
<Select options={[
  { value: 'classification', label: '分类' },
  { value: 'regression',     label: '回归' },
  { value: 'forecasting',    label: '时序预测' },
]} />
```

- [ ] **Step 3: 在创建/编辑 plan 表单条件渲染 TimeSeriesPlanForm**

如果当前文件包含 plan 表单分支（按 task_type 切换 ml/dl 表单），加平行分支：

```jsx
import TimeSeriesPlanForm from '../components/forecasting/TimeSeriesPlanForm'
// ...
{taskType === 'forecasting' && (
  <TimeSeriesPlanForm
    columns={dataset?.columns_info ? Object.keys(dataset.columns_info) : []}
    value={plan?.payload?.time_series}
    onChange={ts => setPlan({ ...plan, payload: { ...plan.payload, time_series: ts } })}
  />
)}
```

如果文件结构不一致，把上述代码片段嵌入到 form section 适当位置。

- [ ] **Step 4: 在 Inspector 区加 ForecastChart**

如果 ModelingTaskDetail 渲染 run inspector，加：

```jsx
import ForecastChart from '../components/forecasting/ForecastChart'
import ForecastMetrics from '../components/forecasting/ForecastMetrics'
// ...
{run?.task_type === 'forecasting' && (
  <>
    <ForecastMetrics metrics={run?.metrics_snapshot || {}} />
    <ForecastChart data={forecastData} />
  </>
)}
```

`forecastData` 通过 `api.get(`/viz/${run.id}/forecast_line`)` 获取（在 `useEffect` 里加载）。

- [ ] **Step 5: build + 浏览器视觉确认**

```bash
cd ml_platform_web && npm run build 2>&1 | tail -10
docker compose -f docker/docker-compose.yml restart frontend && sleep 5
```

打开 http://localhost/ → ModelingTask 创建，确认 task_type 下拉有"时序预测"选项。

- [ ] **Step 6: Commit**

```bash
git add ml_platform_web/src/pages/ModelingTaskDetail.jsx
git commit -m "feat(v3.4): ModelingTaskDetail wires up forecasting task_type"
```

## Task 25: V3Runs filter for ts family + 老页面 deprecated banner

**Files:**
- Modify: `ml_platform_web/src/pages/V3Runs.jsx`
- Modify: `ml_platform_web/src/pages/TSConfig.jsx`
- Modify: `ml_platform_web/src/pages/TSMonitor.jsx`
- Modify: `ml_platform_web/src/pages/TSResults.jsx`
- Modify: `ml_platform_web/src/pages/TimesFM.jsx`

- [ ] **Step 1: V3Runs family filter**

找到 family Select options（如已有 ml/dl 即类似 `[{value:'ml',label:'ML'},{value:'dl',label:'DL'}]`），加：

```jsx
{ value: 'ts', label: '时序' }
```

- [ ] **Step 2: 给四个老页面加 deprecated banner**

每个文件顶部 import：

```jsx
import { Alert } from 'antd'
```

在主 return JSX 顶部插入：

```jsx
<Alert
  type="warning"
  showIcon
  banner
  message="此页面将于 v3.5 移除，请使用 建模任务 → 时序预测 路径"
  style={{ marginBottom: 12 }}
/>
```

四个文件都做同样处理。

- [ ] **Step 3: build**

```bash
cd ml_platform_web && npm run build 2>&1 | tail -10
```

- [ ] **Step 4: Commit**

```bash
git add ml_platform_web/src/pages/V3Runs.jsx ml_platform_web/src/pages/TSConfig.jsx ml_platform_web/src/pages/TSMonitor.jsx ml_platform_web/src/pages/TSResults.jsx ml_platform_web/src/pages/TimesFM.jsx
git commit -m "chore(v3.4): V3Runs ts filter + deprecated banner on old TS pages"
```

---

# M8 — E2E + 文档 + 发布

## Task 26: 准备 sin_wave_daily.csv 测试 fixture

**Files:**
- Create: `examples/data/sin_wave_daily.csv`

- [ ] **Step 1: 用脚本生成 CSV**

```bash
docker exec ml_platform_backend python -c "
import numpy as np, pandas as pd
n = 365
df = pd.DataFrame({
    'ds': pd.date_range('2024-01-01', periods=n, freq='D').strftime('%Y-%m-%d'),
    'y':  np.sin(np.arange(n) * 2 * 3.14159 / 30) + 0.1 * np.random.RandomState(42).randn(n),
})
df.to_csv('/app/examples/data/sin_wave_daily.csv', index=False)
print('rows:', len(df))
"
```

期望：`rows: 365`。

注：因镜像里 `/app/examples` 不一定挂载，文件先生成在容器，再 cp 出来：

```bash
docker cp ml_platform_backend:/app/examples/data/sin_wave_daily.csv examples/data/sin_wave_daily.csv 2>/dev/null || \
docker exec ml_platform_backend python -c "
import numpy as np, pandas as pd
n = 365
df = pd.DataFrame({
    'ds': pd.date_range('2024-01-01', periods=n, freq='D').strftime('%Y-%m-%d'),
    'y':  np.sin(np.arange(n) * 2 * 3.14159 / 30) + 0.1 * np.random.RandomState(42).randn(n),
})
print(df.to_csv(index=False))
" > examples/data/sin_wave_daily.csv
```

- [ ] **Step 2: 校验 row count**

```bash
wc -l examples/data/sin_wave_daily.csv
```

期望：`366` (365 行 + 1 header)。

- [ ] **Step 3: Commit**

```bash
git add examples/data/sin_wave_daily.csv
git commit -m "test(v3.4): sin wave daily CSV fixture for forecasting E2E"
```

## Task 27: 写 13-v3-forecasting.spec.js E2E

**Files:**
- Create: `playwright_test/test/13-v3-forecasting.spec.js`

- [ ] **Step 1: 写 spec**

```javascript
// playwright_test/test/13-v3-forecasting.spec.js
const { test, expect } = require('@playwright/test')
const path = require('path')

const BASE = process.env.BASE_URL || 'http://127.0.0.1'

test.describe('V3 Forecasting end-to-end', () => {
  test.setTimeout(180_000)

  test('create dataset → modeling task (forecasting) → arima baseline → metrics → inspector', async ({ page, request }) => {
    // 1. Upload dataset via API (faster than UI for fixture data)
    const fixturePath = path.join(__dirname, '..', '..', 'examples', 'data', 'sin_wave_daily.csv')
    const uploadRes = await request.post(`${BASE}/api/data/upload`, {
      multipart: { file: { name: 'sin_wave.csv', mimeType: 'text/csv', buffer: require('fs').readFileSync(fixturePath) } },
    })
    expect(uploadRes.ok()).toBeTruthy()
    const ds = await uploadRes.json()

    // 2. Create modeling task with task_type=forecasting via API
    const mtRes = await request.post(`${BASE}/api/v3/tasks`, {
      data: { name: 'e2e-forecasting', dataset_id: ds.id, task_type: 'forecasting' },
    })
    expect(mtRes.ok()).toBeTruthy()
    const mt = await mtRes.json()

    // 3. Bind a TrainingPlan with ARIMA baseline
    const planRes = await request.post(`${BASE}/api/platform/training-plans`, {
      data: {
        name: 'e2e-arima', dataset_id: ds.id, task_type: 'forecasting',
        model_family: 'ts', strategy_type: 'baseline',
        payload: {
          model_tokens: ['arima'],
          time_series: {
            timestamp_col: 'ds', target_col: 'y', series_id_col: null,
            exogenous_cols: [], freq: 'D',
            horizon: 14, lookback: 30,
            validation: { method: 'holdout', test_size: 14, step: 1 },
            interval_levels: [80, 95],
          },
        },
      },
    })
    expect(planRes.ok()).toBeTruthy()

    // 4. Trigger experiment
    const expRes = await request.post(`${BASE}/api/platform/experiments`, {
      data: { modeling_task_id: mt.id, name: 'e2e-exp', strategy_type: 'baseline', model_family: 'ts' },
    })
    expect(expRes.ok()).toBeTruthy()
    const exp = await expRes.json()

    // 5. Poll for completion
    let runs = []
    for (let i = 0; i < 60; i++) {
      const r = await request.get(`${BASE}/api/v3/tasks/${mt.id}/runs`)
      runs = (await r.json()).items || []
      if (runs.length && runs.every(rn => ['SUCCESS', 'FAILED'].includes(rn.status))) break
      await page.waitForTimeout(1500)
    }
    expect(runs.length).toBeGreaterThan(0)
    const ok = runs.find(r => r.status === 'SUCCESS')
    expect(ok, `no SUCCESS run; runs=${JSON.stringify(runs)}`).toBeTruthy()

    // 6. Verify metrics
    expect(ok.metrics_snapshot?.mae).toBeDefined()
    expect(ok.metrics_snapshot?.rmse).toBeDefined()

    // 7. UI: open inspector
    await page.goto(`${BASE}/v3/tasks/${mt.id}`)
    await expect(page.getByText(/ARIMA|arima/)).toBeVisible({ timeout: 15_000 })
  })
})
```

- [ ] **Step 2: 跑 spec**

```bash
cd playwright_test && npx playwright test test/13-v3-forecasting.spec.js --project=chromium 2>&1 | tail -30
```

期望：1 passed。

如有 fail：
- 看后端日志确认 executor 路径
- 看 v3 endpoint 路径名是否一致（`/api/v3/tasks`、`/api/platform/training-plans`、`/api/platform/experiments`）

- [ ] **Step 3: 跑 67+13 全量回归**

```bash
cd playwright_test && npx playwright test --project=chromium 2>&1 | tail -10
```

期望：68/68 通过。

- [ ] **Step 4: Commit**

```bash
git add playwright_test/test/13-v3-forecasting.spec.js
git commit -m "test(v3.4): 13-v3-forecasting E2E spec (dataset→task→arima→metrics→inspector)"
```

## Task 28: 文档更新 + 版本号

**Files:**
- Modify: `doc/功能说明.md`
- Modify: `doc/系统架构.md`
- Modify: `ml_platform_web/package.json`
- Modify: `ml_platform/app/main.py`（version）

- [ ] **Step 1: 在 功能说明.md 加 forecasting 章节**

末尾追加：

```markdown
## 时序预测 (forecasting) — v3.4 新增

V3 平台第三个一等 family `ts`，与 ml/dl 同构走 ModelingTask → Experiment → Run 链路。

支持模型：
- ARIMA / ETS — `statsmodels`，原生预测区间
- LSTM-forecaster / TCN-forecaster — pytorch direct multi-step
- TimesFM 1.0 — Google 基础模型，3.10 venv 子进程

任务类型：`forecasting`；数据元数据（时间列、目标列、horizon、validation 策略）走 TrainingPlan.payload.time_series。

老路径 `/ts/tasks`、`/timesfm` 已 deprecated，将于 v3.5 移除。
```

- [ ] **Step 2: 系统架构.md 把 family 列表更新为 ml/dl/ts**

grep `ml.*dl` 找到现有 family 描述段，把 "ml + dl" 换成 "ml + dl + ts"。

- [ ] **Step 3: 版本号 → 3.4.0**

```bash
sed -i.bak 's/"version": "3.3.0"/"version": "3.4.0"/' ml_platform_web/package.json
grep version ml_platform_web/package.json
```

- [ ] **Step 4: 后端 /health.version**

```bash
grep -n 'version.*=' ml_platform/app/main.py | head
```

修改 version 字符串到 `3.4.0`。

- [ ] **Step 5: 全量 67+13 回归 + 健康检查**

```bash
docker compose -f docker/docker-compose.yml restart backend frontend && sleep 15
curl -s http://localhost:8000/health
cd playwright_test && npx playwright test --project=chromium 2>&1 | tail -10
```

期望：`{"status":"ok","version":"3.4.0"}` + 68/68。

- [ ] **Step 6: Commit + Tag**

```bash
git add doc/功能说明.md doc/系统架构.md ml_platform_web/package.json ml_platform/app/main.py
rm -f ml_platform_web/package.json.bak
git commit -m "release(v3.4.0): time-series → V3 platform integration"
git tag v3.4.0
```

---

## 完成验收清单

- [ ] 所有 28 个 Task 全部 Commit
- [ ] `ml_platform/tests/` 单测全绿（含 6 个新测试文件）
- [ ] Playwright 68/68 全绿
- [ ] `/health.version` 报 3.4.0
- [ ] `curl /api/v3/ts/models` 列出 5 个模型，timesfm_1 的 `available` 字段正确反映 venv 状态
- [ ] 老 `/ts/tasks` 路径仍可访问（带 deprecated banner）
- [ ] V3 ModelingTask 创建表单可选 task_type=forecasting，可绑定 ts family plan
