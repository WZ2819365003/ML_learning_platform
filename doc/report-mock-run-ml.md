# lightgbm_regressor · 分报告 ｜ 样稿 v2（树模型）

> baseline · 默认超参数 · 5 折交叉验证 · Trial 2

**交叉验证 RMSE 72.8，与最优的 xgboost 只差 0.3，分不出高下；五折稳定，没有离群折。**

## 训练过程

树模型不按轮迭代，没有收敛曲线，看五折的表现。

**[图 · 五折 RMSE]** 横向条形 ×5，均值虚线
　悬停：折 · RMSE · MAE · R²
　图注：五折落在 71.6–74.1，极差 2.4，约为均值的 3%；第 4 折最差、第 5 折最好，但没有哪一折明显掉队。

折间标准差 0.93，变异系数 1.3%。数据划分均匀，模型表现不依赖具体切分。<<这说明什么，一句>>

## 训练结果

RMSE 72.8、MAE 54.7，分别是平均负荷的 0.8% 和 0.6%；R² 0.997。

**[图 · 实际值 vs 预测值 ‖ 残差分布]** 左：留出集两条线；右：残差直方图（并排）
　悬停：序号 · 实际 · 预测 · 残差 ｜ 区间 · 样本数
　图注：两条线在平段几乎重合，偏差集中在尖峰；残差近似对称，RMSE/MAE 比 1.33 略高于正态的 1.25，有少量大偏差样本但不成长尾。

**[图 · 最依赖的 8 个特征]** SHAP 横向条形
　悬停：特征 · 平均绝对 SHAP · 占首位 %
　图注：load_lag_1 一个特征的贡献是第二名 hour_cos 的 15 倍。

模型本质上在做短期外推——用上一时刻的负荷预测当前时刻，日内周期只做修正。与 xgboost 的特征结构一致，差异来自拟合细节，不是用了不同的信息。<<这个依赖在什么场景下失效，一句>>
