# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/
ml_platform/timesfm_env/
.tmp_pytest*/
.tmp_pytest_run/
**/.tmp_pytest*/
**/.pytest_cache/

# ML Platform runtime data — all storage now lives in Docker volumes (MySQL/MinIO)
ml_platform/mlruns/
ml_platform/storage/uploads/
ml_platform/storage/models/
ml_platform/storage/logs/
ml_platform/storage/predictions/
ml_platform/storage/*.db

# Node
node_modules/
ml_platform_web/dist/
ml_platform_web/node_modules/
ml_platform_web/vite.*.log

# IDE / Editor plugins
.idea/
.vscode/
.trae/
*.swp
*.swo

# Test artifacts (regenerated by test runner)
test-results/
playwright-report/
.playwright-cli/
.pytest_cache/
playwright_test/artifacts/
output/

# OS
.DS_Store
Thumbs.db

# Environment
.env
# Local logs are covered by storage/logs/ and vite.*.log above.

# Docker packaging/runtime artifacts
docker/*.tar
docker/runtime/

# Old docker release packages
docker/*.zip
docker/ML_platform_release/
docker/minio_seed/
docker/runtime_data/

# MCP config contains SSH credentials — do NOT commit
.mcp.json

# Local dev env overrides
.env.local
ml_platform_web/.env.local

# Server-only deployment credentials
docker/.deploy_secrets

# 本地产物 / 临时工作区（不入库）
screenshots/
.claude/worktrees/
ml_platform/storage/backups/
