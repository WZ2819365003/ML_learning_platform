# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/
ml_platform/timesfm_env/
.tmp_pytest*/
.tmp_pytest_run/
**/.tmp_pytest*/
**/.pytest_cache/

# ML Platform runtime data
# uploads/ and models/ can be large binaries — track intentionally with git add -f if needed
ml_platform/mlruns/

# Keep storage directory structure in git (uploads, models, logs, db are tracked)
# Uncomment below to ignore large files if needed:
# ml_platform/storage/uploads/
# ml_platform/storage/models/
# ml_platform/storage/logs/
# ml_platform/storage/*.db

# Node
node_modules/
ml_platform_web/dist/
ml_platform_web/node_modules/
ml_platform_web/vite.*.log

# IDE / Editor plugins
.idea/
.vscode/
.trae/
*.swp
*.swo

# Test artifacts (regenerated by test runner)
test-results/
playwright-report/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Environment
.env
# *.log  # removed: storage/logs/*.log should be tracked

# Docker packaging/runtime artifacts
docker/*.tar
docker/runtime/

# Old docker release packages
docker/*.zip
docker/ML_platform_release/
docker/minio_seed/
docker/mysql_init/01_data.sql
docker/runtime_data/
